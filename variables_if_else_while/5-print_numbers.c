#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 ** main - prints all single digit numbers of base 10
 *
 ** Return: Always 0 (Success)
 */
int main(void)
{ 
	for (int i = 0; i < 10; i++)
	{
		printf("%d ", i);
	}
	printf("\n");
	return (0);
}
