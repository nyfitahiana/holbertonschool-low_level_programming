#include "main.h"
#include <stdio.h>
/**
 * main - Prints the neme of a program
 * @argc: the number of arguments passed to the program
 * @argv: an array of strings
 * Return: Always 0 Success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
